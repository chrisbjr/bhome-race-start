services:
  reaction-timer:
    image: nginx:alpine
    container_name: reaction-timer
    ports:
      - "${PORT:-8080}:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
